import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Gaudiya Kirtan Coming Soon",
  description: "Sign up to the Gaudiya Kirtan mailing list to get notified when the upcoming app is released and to receive special songs to sing for various festival days throughout the year.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
